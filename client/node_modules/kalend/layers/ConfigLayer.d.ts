import { Callbacks, Config } from '../common/interface';
import { KalendProps } from '../index';
export declare const createConfig: (props: KalendProps) => Config;
export declare const createCallbacks: (props: KalendProps) => Callbacks;
declare const ConfigLayer: (props: KalendProps) => any;
export default ConfigLayer;
