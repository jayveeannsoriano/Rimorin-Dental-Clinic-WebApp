import { CALENDAR_VIEW, CalendarEvent, Config } from '../index';
import { DateTime } from 'luxon';
import { ItemRowLayoutResult } from '../types';
export declare const calculatePositionForHeaderEvents: (events: CalendarEvent[], width: number, calendarDays: DateTime[], config: Config, selectedView: CALENDAR_VIEW) => ItemRowLayoutResult;
