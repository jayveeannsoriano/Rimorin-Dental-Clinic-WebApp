import { CalendarEvent, Config } from '../index';
import { DateTime } from 'luxon';
export declare const formatToDateKey: (date: DateTime, timezone?: string) => string;
export declare const parseToCalendarDays: (calendarDays: DateTime[] | string[]) => DateTime[];
export declare const isEventFloating: (event: CalendarEvent) => boolean;
export declare const sortEvents: (events: CalendarEvent[]) => CalendarEvent[];
/**
 * Set UTC for floating dates or use config timezone
 * @param timezone
 * @param isFloating
 */
export declare const parseTimezone: (timezone: string, isFloating?: boolean) => string;
/**
 * Get offset from top by diff between top position and event starting position
 * @param event
 * @param config
 */
export declare const calculateOffsetTop: (event: CalendarEvent, config: Config) => number;
/**
 * Get event height by diff in end and start date
 * @param event
 * @param config
 */
export declare const calculateEventHeight: (event: CalendarEvent, config: Config) => number;
