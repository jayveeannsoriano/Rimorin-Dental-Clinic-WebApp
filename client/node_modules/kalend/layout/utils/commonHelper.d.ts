import { ItemRowLayoutResult, KeyedEventsExtended } from '../types';
import { DateTime } from 'luxon';
export declare const DEFAULT_ROW_LAYOUT_RESULT: {
    positions: never[];
    overflowingEvents: never[];
    headerOffsetTop: number;
};
/**
 * Use for month view and header events
 * @param events
 * @param width
 * @param calendarDays
 * @param timezone
 * @param maxEventsVisible
 * @param isHeaderEvents
 * @param overflowingEvents
 */
export declare const getRowLayout: (events: KeyedEventsExtended, width: number, calendarDays: DateTime[], timezone: string, maxEventsVisible: number, isHeaderEvents?: boolean, overflowingEvents?: any) => ItemRowLayoutResult;
