import { CALENDAR_VIEW, CalendarEvent, Config } from '../index';
import { DateTime } from 'luxon';
import { KeyedEvents, RowLayoutResult } from '../types';
export declare const parseSingleDay: (event: CalendarEvent, config: Config, preparedEventsPrev: KeyedEvents, originDate: string) => {
    [x: string]: CalendarEvent[];
};
export declare const prepareMultiDayEvents: (events: CalendarEvent[], config: Config, breakPointDate?: string, view?: CALENDAR_VIEW) => KeyedEvents;
export declare const getMonthViewLayout: (events: CalendarEvent[], width: number, calendarDays: DateTime[], config: Config, maxEventsVisible: number, isHeaderEvents?: boolean) => RowLayoutResult;
