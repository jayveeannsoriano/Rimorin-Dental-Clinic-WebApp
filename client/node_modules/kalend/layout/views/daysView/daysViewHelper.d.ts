import { CalendarEvent, Config } from '../../index';
import { DayViewResult, GroupedLayouts, LayoutEvent } from '../../types';
/**
 * Check if event is overlapping with other events in one group of layouts
 * @param item
 * @param refFirstCoordinate
 * @param refLastCoordinate
 */
export declare const checkOverlappingYCoordinates: (item: LayoutEvent, refFirstCoordinate: number, refLastCoordinate: number) => boolean;
/**
 * Get basic layout with offset top and event height
 * @param events
 * @param config
 */
export declare const getEventsLayouts: (events: CalendarEvent[], config: Config) => LayoutEvent[];
/**
 * Group events according to their position on y-axis
 * @param events
 */
export declare const groupEvents: (events: LayoutEvent[]) => GroupedLayouts;
export declare const parseToDayViewResult: (dateKey: string, eventWidth: number, tableWidth: number, tableSpace: number, index: number, groupItem: LayoutEvent) => DayViewResult;
