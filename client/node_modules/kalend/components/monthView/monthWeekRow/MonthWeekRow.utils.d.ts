import { CalendarEvent, Config } from '../../../common/interface';
import { DateTime } from 'luxon';
export declare const getMonthRows: (calendarDays: DateTime[]) => DateTime[][];
export declare const sortByLength: (events: CalendarEvent[]) => CalendarEvent[];
export declare const calculateMonthPositions: (events: any, width: number, calendarDays: DateTime[], config: Config, maxEventsVisible: number, setContext?: any) => any;
