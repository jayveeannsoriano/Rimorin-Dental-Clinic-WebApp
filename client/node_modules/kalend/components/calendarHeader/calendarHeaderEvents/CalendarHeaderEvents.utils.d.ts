import { CalendarEvent } from '../../../common/interface';
import { DateTime } from 'luxon';
export declare const stretchHeaderEventTimes: (event: CalendarEvent, timezone: string) => CalendarEvent;
export declare const calculatePositionForHeaderEvents: (events: any, width: number, calendarDays: DateTime[], timezone: string, setContext?: any) => any;
