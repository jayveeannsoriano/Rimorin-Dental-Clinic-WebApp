import { CALENDAR_NAVIGATION_DIRECTION, CALENDAR_VIEW, WEEKDAY_START } from '../common/enums';
import { DateTime } from 'luxon';
/**
 * Calculate new calendar days
 * @param calendarDays
 * @param calendarView
 * @param direction
 * @param weekDayStart
 * @param dispatchContext
 */
export declare const getNewCalendarDays: (calendarDays: DateTime[], calendarView: CALENDAR_VIEW, direction: CALENDAR_NAVIGATION_DIRECTION, weekDayStart: WEEKDAY_START, dispatchContext?: any) => DateTime[];
export declare const navigateToToday: (selectedView: CALENDAR_VIEW, setContext: any, weekDayStart: WEEKDAY_START, dateNow?: DateTime) => Promise<void>;
